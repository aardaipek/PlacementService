import { UpdateUniversityDto } from './update.university.dto';

describe('UpdateUniversityDto', () => {
  it('should be defined', () => {
    expect(new UpdateUniversityDto()).toBeDefined();
  });
});
